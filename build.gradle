dependencies {
	compile project(':opt4j-core')
}

sourceSets {
    main {
        java {
            srcDir 'src'
            exclude '**/Opt4JOperatorsActivator.java'
            exclude 'test/**'
       }
   }
}
